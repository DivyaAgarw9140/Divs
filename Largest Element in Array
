class Solution {
  public:
    int largest(vector<int> &arr) {
       int i,n;
        n=sizeof(arr)/sizeof(arr[0]);
       
        int largest;
        largest=arr[0];
        for(i=0;i<n;i++)
        {
        if(largest<arr[i+1])
        largest=arr[i+1];
        }
    return largest;
}

};